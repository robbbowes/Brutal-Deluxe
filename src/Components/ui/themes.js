import { createMuiTheme } from '@material-ui/core/styles';

export const CyberpunkTheme = createMuiTheme({
    palette: {
        primary: {
            light: '#757ce8',
            main: '#00e3ef',
            dark: '#002884',
            contrastText: '#fff',
        }
    }
})